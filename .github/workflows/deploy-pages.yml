name: Deploy dual-agent web to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - "015-dual-agent-chat/web/**"
      - ".github/workflows/deploy-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: 015-dual-agent-chat/web/package.json

      - name: Install & build
        working-directory: 015-dual-agent-chat/web
        env:
          VITE_BASE: ./
        run: |
          npm install
          npm run build
          # Ensure SPA fallback for project pages
          cp dist/index.html dist/404.html

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 015-dual-agent-chat/web/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
